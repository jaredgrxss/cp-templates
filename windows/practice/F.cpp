#include <bits/stdc++.h>
using namespace std;


int main() {
	ios::sync_with_stdio(0); cin.tie(0);
	int tc = 1; 
	for (int i = 0; i < MAXN; i++) dsu[i] = i, sz[i] = 1, sets[i].push_back(i);
	// cin >> tc;
	for (int tt = 0; tt < tc; tt++) {
		
	}
}
