#include <bits/stdc++.h>
using namespace std;
#define int long long

int32_t main() {
	ios::sync_with_stdio(0); cin.tie(0);
	int tc = 1; 
	cin >> tc;
	for (int t = 0; t < tc; t++) {
		int n, k; cin >> n >> k;
		vector<int> arr(n);
		for (int& i : arr) cin >> i;

		
		
	}
	return 0;
}
