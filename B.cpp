#include <bits/stdc++.h>
using namespace std;
#define int long long

int32_t main() {
	ios::sync_with_stdio(0); cin.tie(0);
	int tc = 1; 
	cin >> tc;
	for (int t = 0; t < tc; t++) {
		int n, m, d; cin >> n >> m >> d;
		vector<int> op(m);
		for (int& i : op) cin >> i;
		
		


	}
	return 0;
}
